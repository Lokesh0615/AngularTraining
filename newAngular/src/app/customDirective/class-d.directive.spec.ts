import { ClassDDirective } from './class-d.directive';

describe('ClassDDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassDDirective();
    expect(directive).toBeTruthy();
  });
});
