import { RendererListenDirective } from './renderer-listen.directive';

describe('RendererListenDirective', () => {
  it('should create an instance', () => {
    const directive = new RendererListenDirective();
    expect(directive).toBeTruthy();
  });
});
